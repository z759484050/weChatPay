import { AppConfig } from "remax/wechat";

const config: AppConfig = {
  pages: ['pages/index/index'],
  window: {
    navigationBarTitleText: '境外游礼包',
    navigationBarBackgroundColor: '#E3482E',
    navigationStyle:'custom'
  }
};

export default config;
